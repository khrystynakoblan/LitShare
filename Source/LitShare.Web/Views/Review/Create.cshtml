@model LitShare.Web.Models.ReviewViewModel
@{
    ViewData["Title"] = "Залишити відгук";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
            <div class="card p-4 p-md-5 shadow-sm border-0">
                <h4 class="fw-bold text-center mb-4">Відгук про @Model.ReviewedUserName</h4>

                <form asp-action="Create" method="post" id="reviewForm">
                    <input type="hidden" asp-for="ReviewedUserId" />
                    <input type="hidden" asp-for="ReviewedUserName" />

                    <div asp-validation-summary="ModelOnly" class="text-danger mb-3 small"></div>

                    <div class="text-center mb-4">
                        <label class="fw-bold d-block mb-2">Ваша оцінка</label>
                        <div class="d-flex justify-content-center gap-2" id="star-rating">
                            @for (int i = 1; i <= 5; i++)
                            {
                                <i class="bi bi-star fs-1 text-warning star-btn" style="cursor:pointer;" data-value="@i"></i>
                            }
                        </div>
                        <input type="hidden" asp-for="Rating" id="ratingInput" />
                        <span asp-validation-for="Rating" class="text-danger small"></span>
                    </div>

                    <div class="mb-4">
                        <label class="fw-bold mb-2" asp-for="Text">Коментар</label>
                        <textarea asp-for="Text" class="form-control" rows="5" placeholder="Поділіться враженнями від обміну..."></textarea>
                        <span asp-validation-for="Text" class="text-danger small"></span>
                    </div>

                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-dark py-2 fw-bold">Надіслати відгук</button>
                        <a asp-controller="Profile" asp-action="GuestProfile" asp-route-id="@Model.ReviewedUserId" class="btn btn-outline-dark py-2">Скасувати</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/rating.js" asp-append-version="true"></script>
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}